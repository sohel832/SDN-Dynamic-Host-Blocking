Dynamic Host Blocking System (SDN Mini Project) 👤 Student InformationName: Sohel Mahibubasa Chapparband SRN: PES2UG25CS826 Section: F 📌 Project OverviewThis project develops a Dynamic Host Blocking System using a POX controller that can actively isolate network nodes based on real-time administrative input. It evolves the "black hole" concept into a dynamic security tool where policies are applied without restarting the controller or the Mininet topology. 🛠️ Tech StackEmulator: Mininet (Python 3) SDN Controller: POX Protocol: OpenFlow v1.0 Switch: Open vSwitch (OVS) 📂 Project Structuredynamic_blocking.py: The POX controller logic containing the block_host function. topology.py: Custom 3-host, 1-switch Mininet topology. 💻 Usage Instructions1. Start the POX ControllerBash./pox.py log.level --DEBUG dynamic_blocking
2. Launch the Mininet TopologyBashsudo mn --custom topology.py --topo simpletopo --controller remote,ip=127.0.0.1,port=6633
3. Dynamic Blocking CommandIn the POX terminal, use the following to isolate a host:PythonPOX> core.dynamic_blocking.block_host("10.0.0.1")
📊 Experimental ResultsMetricResultTechnical ReasonInitial State0% Packet LossNo security policies applied; flows installed normally. Scenario 1 (Block h1)66% droppedHost 10.0.0.1 isolated; controller refuses flow setup. Scenario 2 (Block h2)66% droppedBoth h1 and h2 isolated; only h3 remains reachable. 🛡️ Technical ProofVerification is performed via the switch flow table:mininet> sh ovs-ofctl dump-flows s1 The output confirms that blocked hosts have n_packets=0 for ICMP traffic, proving the controller is "black-holing" their traffic at the control plane level. 
